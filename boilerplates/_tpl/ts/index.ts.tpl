import './index.%suffix%';

const root = document.getElementById('root');

if (root) {
    root.innerHTML = (`
        <div class="app">
            <h1 class="title">Hello Tex App</h1>
        </div>
    `);
}
